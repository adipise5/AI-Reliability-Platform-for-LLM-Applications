[project]
name = "dataset-management"
version = "0.1.0"
description = "AI Reliability Platform — Dataset Management service"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115",
  "uvicorn[standard]>=0.30",
  "pydantic>=2.7",
  "pydantic-settings>=2.3",
  "sqlalchemy[asyncio]>=2.0",
  "asyncpg>=0.29",
  "alembic>=1.13",
  # arp-auth-client (../../libs/auth-client) is NOT listed here: pip does
  # not reliably resolve relative `file:` dependency specs in pyproject.toml.
  # Install it explicitly first — see README.md "Running locally".
]

[project.optional-dependencies]
dev = [
  "pytest>=8.2",
  "pytest-asyncio>=0.23",
  "httpx>=0.27",
  "respx>=0.21",
  "aiosqlite>=0.20",
  "ruff>=0.6",
  "mypy>=1.11",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/dataset_management"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
line-length = 110
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.flake8-bugbear]
extend-immutable-calls = ["fastapi.Depends", "fastapi.Query"]

[tool.mypy]
python_version = "3.12"
packages = ["dataset_management"]
mypy_path = "src"
strict = true

[[tool.mypy.overrides]]
module = "alembic.*"
ignore_missing_imports = true
